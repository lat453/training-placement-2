class Solution:
    def subtractProductAndSum(self, n):
        digits = [int(d) for d in str(n)]
        product = 1
        for d in digits:
            product *= d
        return product - sum(digits)
